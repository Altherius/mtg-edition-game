{% extends 'base.html.twig' %}

{% block title %}Play - {{ parent() }}{% endblock %}

{% block body %}
    {% if app.user is null %}
    <div class="alert alert-warning text-center">You are not logged in ! Your high scores will not be saved.</div>
    {% endif %}
    <div id="app" {{ stimulus_controller('quiz') }}></div>
{% endblock %}
